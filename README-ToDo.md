# Design-level decisions:
  * How to handle "illegal instruction" in step! ... throw an error?
    (currently just prints to stdout)

* Important implementation reminders
  * Flags aren't being updated at all in the hardware module
